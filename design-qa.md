# QuotaView Interface Design QA

> 文档编号：`QV-EVIDENCE-DESIGN-QA-001`
>
> 证据状态：`Active`
>
> 当前生产基线：[最新正式版](VERSION_HISTORY.md#当前最新版本)
>
> 当前开发：[0.5.0 Build 3，主应用与开发台同步运行](HANDOFF.md#工作区与版本定位)；最近同步：2026-09-13

本文件只记录产品所有者已经明确给出的视觉/交互结论和仍待验收的当前范围。
自动化结果记录在对应功能规格，公开版本的签名、公证和发布证据属于 `VERSION_HISTORY.md`；设计规范属于
[界面规范](docs/design/QUOTAVIEW_UI_RULES.md)。旧版本的局部 Passed 不能推导为当前完整矩阵通过。

## 当前待验收

| 范围 | 状态 | 说明 |
|---|---|---|
| 0.5.0 Build 3 纯黑底色与审计修复 | 已启动 / 待体验 | 主应用与开发台同时运行相同源码，完成展开 / 缩略保持纯黑底色；此前呼出中断隐藏、回弹悬停修复一并纳入。AppKit 自动检查不能替代人工确认 |
| 0.5.0 / LONG-020 扩展验收 | 等待实机矩阵 | 多屏、Intel GPU、真实长时间运行及辅助功能组合未由本次动效确认覆盖 |
| 0.4.7 Build 3 设置调整 | 等待用户验收 | 协议菜单右对齐、七个彩色圆角侧栏图标；中英文、深浅色、启用/禁用及辅助功能 |
| 当前单任务灵动岛完整矩阵 | 等待完整矩阵 | 历史 0.3.6 Demo 与 Build 2 动效均只有各自限定范围的确认；生产设置页和九状态完整交叉矩阵未单独确认 |
| 0.3.5 自动更新独立设置行 | 等待完整矩阵 | 已按原生“左文右 Switch”设置行发布；深浅色、键盘、VoiceOver 与 Increase Contrast 未逐项记录 |
| 0.3.4 Build 3 Token/成本修正 | 等待完整矩阵 | 完整 16 列补齐与成本图底部同字号样式已实现；未记录完整视觉结论 |

完整矩阵包含：浅色/深色/跟随系统、磨砂/清透、中英文、可用/不可用/刷新、
Hover/Pressed/Disabled/键盘、Reduce Motion、Increase Contrast 和 VoiceOver。

## 已确认的局部结论

| 版本 | 结论 | 产品所有者确认范围 |
|---|---|---|
| `0.5.0 Build 2` 真实开发应用 | Passed（动效基线） | 2026-09-13 用户确认“目前的动效我比较满意了可以先固定在这个版本”；冻结呼出、展开 / 缩略、隐藏及内容共同回弹的正常表现。随后审计修复与 Build 3 纯黑底色不纳入此次人工结论；详见[动效规格](docs/design/quotaview-island-motion-0.5.0.md) |
| `0.4.7 Build 3` 内容开发台 | Passed（当前手动检查） | 2026-09-11 用户确认“目前手动检查通过”；限于此次灵动岛内容检查，不推导完整辅助功能或真实生命周期计时通过；设置新图标不在此结论内 |
| `0.4.6 Build 1` | Passed（本轮真实任务） | 2026-09-05 用户确认真实五步任务视觉无问题：思考、执行、文件编辑、计划进度与真实完成回执；计划全完成后继续执行 12 秒才结束。等待确认、上下文压缩和完整辅助功能矩阵不在本次结论内 |
| `0.3.6` 动画 Demo | Passed | 波澜光晕复用现有灵动岛外壳、保持圆形轮廓、节奏加快 50% |
| `0.3.4 Build 1` | Passed（局部） | 主额度内下次重置、Spark 紧凑布局、中性进度条、无重复订阅方案 |
| `0.3.3 Build 3` | Passed（局部） | 16 列 Token 网格、左上占位、五级单色、0.5 秒 Tooltip、顶部固定收展 |
| `0.1.5` | Passed（局部） | 清透玻璃材质方向 |

## 被后续版本替代的结论

- `0.3.5 Build 4` 的居中自动更新设置布局未获验收，由 Build 5 的独立设置
  行替代；
- `0.3.4 Build 2` 的“半年只显示有效格、无占位”解释被产品所有者否定，
  Build 3 恢复连续日期和每行 16 格；
- 更早版本的按钮、重置流程、设置窗口等等待项已被后续生产实现覆盖，不再
  作为当前待办；如调查历史回归，以对应 tag 和 Git 历史为准。

## 记录规则

- 只记录产品所有者明确确认的版本与范围；截图、构建通过或批准发布不自动
  等同于完整视觉验收。
- Prototype 证据保存在对应 Prototype 中，不回填生产结论。
- 新结论更新本文件的当前待验收表；不复制代码验证和 Release 资产细节。
