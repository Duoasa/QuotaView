# QuotaView 执行约束与任务路由

用户当前要求优先。当前代码说明实际行为，已接受规格说明目标行为；修复被
请求的偏差，不用历史文档覆盖开发源码。公开稳定版由
[版本历史](VERSION_HISTORY.md#当前最新版本) 定位，开发最新版由
[Handoff](HANDOFF.md#工作区与版本定位) 定位；两者可能暂用相同配置版本号。

## 按任务读取

修改前用 Git 核对目标工作区和已有改动。已知任务与文件时直接定位相关章节，
无需先阅读所有入口、完整发布史或整份设计规范。

| 当前任务 | 相关上下文 |
|---|---|
| 解释、拼写、链接或局部文档修复 | 目标段落及直接引用 |
| 继续开发、工作区不明、区分稳定与开发版 | [Handoff](HANDOFF.md) 的工作区、开发最新版与下一步 |
| 已有功能修改或修复 | 受影响代码、测试与所属规格；未知 Spec 时查[注册表](docs/specs/README.md) |
| 新功能、架构边界或 SDD 流程变化 | [SDD 流程](docs/specs/DEVELOPMENT_PROCESS.md) 与所属规格 |
| UI、资源、布局、文字或交互 | [界面规范](docs/design/QUOTAVIEW_UI_RULES.md) 的相关章节及功能规格 |
| 灵动岛特效适配 | [效果契约](docs/specs/PROGRESS_EFFECT_ADAPTATION.md) |
| 选择测试、构建和交付检查 | [验证规则](docs/workflow/VALIDATION.md) 的对应变更类型 |
| 版本身份、签名、公证、发布、appcast 或撤回 | [发布规则](docs/workflow/RELEASE.md) 与版本历史当前节 |
| 回溯视觉验收或旧决策 | 按需读 [design-qa.md](design-qa.md) 或规格链接的历史记录 |

这些是读取条件，不是每次执行的步骤；已有足够上下文时不重复读取。

## 执行与完成

- 用户要求实施或修复，即授权在该范围内编辑、审查、运行合适的本地验证、
  修复本次引入的失败并复验。沿用会话已有授权，不为规格落地或每次重试
  再次询问；单纯讨论或接受规格不自动变成生产实施任务。
- 开始时明确可观察结果、约束和完成条件。持续做到请求结果存在、必要检查
  完成、相关记录更新；第一版实现和阶段结束不是默认停止点。
- 缺失信息确实影响正确性且无法从现有材料确定时才提问；完成不依赖该答案
  的工作。具体操作被权限或外部条件阻塞时准确报告，不提前停止独立工作。
- 保护已有未提交改动、用户文件、账户和设备数据。不擅自 reset、clean、
  大范围暂存、提交、推送、安装替换、卸载、清空数据或改变版本身份。
- 产品方向改变、Prototype 迁入生产和发布需对应授权。精确版本 appcast
  准入的整链路授权含义按[发布规则](docs/workflow/RELEASE.md#应用内自动更新序列准入门禁)
  执行；不将其扩展到未授权的版本、渠道或操作。
- 只同步受影响的事实：规格维护目标与证据，注册表维护索引和状态，Handoff
  维护当前工作，版本历史维护已发生的发布。历史“下一步”不构成当前授权。

## 产品与数据约束

- 保持紧凑、数据优先的原生 macOS 工具；稳定版保留单任务灵动岛，历史多任务
  Preview 不静默迁入。优先复用现有令牌、系统控件及 AppKit 玻璃组合。
- 主面板和重置页使用 Asta Sans；设置使用系统字体、语义色和系统强调色。
  主面板只保留一层整面实时取样玻璃，设置保持可读的系统表面；不引入私有
  光学 API 或屏幕录制权限。详细几何、材质、图标与交互按 UI 路由查阅。
- 数据缺失不伪造为零或成功，订阅来自真实快照，未知值用占位符。连接圆点
  只表示最近数据获取是否有效。计划进度不是耗时或工具调用数；只有可信的
  真实完成事件进入 100%。
- 额度重置保持演示，不调用 `account/rateLimitResetCredit/consume`。
  不硬编码额度、重置次数、更新时间或语言；业务文案、Tooltip、辅助功能
  文本使用 `AppCopy`，支持中文与 English。
- 禁用功能停止对应后台工作；Prototype 和测试夹具与生产 Target、真实账户
  数据和发布渠道隔离。

## 验收与开发工具

- Codex 完成代码审查、状态逻辑、自动化和构建；视觉、交互由用户验收。
  未经本任务要求不主动截图、不添加自动展开/点击或 UI QA 入口；没有用户
  结论不记录“视觉通过”。等待体验判断不阻止独立工作继续。
- [灵动岛内容开发台](Prototypes/IslandTextConsole/README.md) 是永久工具；
  使用或修改时读目录内 `AGENTS.md`。保留手动控制及可停止演示，不按临时
  QA 删除，不将模拟入口迁入正式应用；生成包和缓存保持忽略。
- 临时虚拟数据仅允许显式 Debug-only 注入，界面、Tooltip、辅助功能标注
  DEBUG，源码标记 `DEBUG-ONLY-MOCK`。任务结束清理临时 QA；提交、推送或
  发布前移除生产虚拟注入及标记，恢复真实数据控制。生产 `Sources` 搜索
  `DEBUG-ONLY-MOCK|DEBUG MOCK|仅用于调试` 应无匹配；隔离开发台和测试夹具保留。

## CodexBar 参考边界

只有用户明确要求参考 CodexBar，或新增模块需要方案预检时，才读取
`docs/reference/codexbar-macos-design-reference.md`；普通修复和已有模块调整
不触发。新增模块实施前，说明是否有对应方案、适配代价和不应照搬之处，
取得采用该方案或按 QuotaView 自身约束设计的明确确认。同一会话已确认则
继续，不重复询问；参考不得覆盖当前产品、隐私与发布约束。
